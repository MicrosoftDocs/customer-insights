Saving an export doesn't run the export immediately. The export runs with every [scheduled refresh](../schedule-refresh.md). You can also [export data on demand](../export-manage.md#run-exports-on-demand).